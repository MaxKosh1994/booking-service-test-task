export enum API_ROUTES {
  BASE = '/api',
  HEALTH = '/health',
  AUTH = '/auth',
  USERS = '/users',
  EVENTS = '/events',
  BOOKINGS = '/bookings',
  ADMIN = '/admin',
  DOCS = '/docs',
  RESERVE = '/reserve',
}
